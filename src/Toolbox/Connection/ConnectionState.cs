using System;
using System.Collections.Generic;
using System.Text;

namespace Toolbox.Connection
{
    public enum ConnectionState
    {
        Disconnected,
        Connecting,
        Connected,
        Disconnecting
    };
}
